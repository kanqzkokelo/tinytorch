# Roadmap: beat llama.cpp everywhere (M9–M11)

North star: better tok/s, correctness gates, startup, memory than llama.cpp;
much broader hardware support; speculative decoding (ngram first, DFlash2-capable).

## M9 — decode throughput round 2
1. Graph capture for PLE models: precompute full PLE row in embed_token BEFORE
   the captured region (row depends only on token embedding), then capture
   forward_layers without blocking copies. See graph-capture diagnosis agent's
   findings (pending).
2. Revisit kernel fusion post-graphs (fusion lost to launch overhead before
   graphs existed; re-measure with capture active).
3. E2B-shape GEMV tuning: hd=512 flash path, ffn 12288 tail layers.
4. Gate: ≥1.5× llama.cpp on E2B tg-128 via tools/bench_gemma4_e2b.sh;
   hold ≥287 tok/s Qwen flagship.

## M10 — speculative decoding
Research verdicts (2026-08-26, agent-sourced):
- DFlash2 = trained block-diffusion drafter w/ KV-injection conditioning +
  Viterbi candidate-path selector (arXiv:2602.06036, z-lab/dflash, vLLM PR
  #52816, llama.cpp `draft-dflash`). Needs distilled drafter PER TARGET MODEL.
  Parked until we host 7B+ targets; plumbing below keeps the door open.
- ngram family = draftless, ~100 LOC, zero model cost. llama.cpp variants:
  simple (n=12 window, propose m), map-k (hash→mgram, min-hits), map-k4v,
  mod (LCG rolling hash, 16MB const). Wins: code-edit/summarize/RAG 2–4×;
  open-ended chat ≈0. Lookahead/Jacobi: bad fit for 4GB single-batch.

Build order:
1. Spec-decode skeleton: linear-chain draft → ONE batched verify forward
   (k+1 positions, causal) → greedy accept loop → KV rollback to first mismatch
   (pattern: llama_memory_seq_rm equivalent = reset d_pos + memset tail slots).
   Engine needs: multi-position forward (already exists = prefill path),
   per-slot logits extraction, cache truncation helper.
2. ngram-simple table: last-n window match over prompt+generated ring buffer.
3. Adaptive draft length from accept-rate stats (skip drafting when cold).
4. Graph capture per verify shape k+1 (launch overhead would eat the gain).
5. Rejection sampling for temp>0 (later; CLI is greedy-first).
6. DFlash2/EAGLE drafter interface: pluggable "draft source" behind the same
   verify loop — implement interface now, drafter loaders later.

## M11 — hardware breadth
- CPU backend: dequant_ref.c already portable; add CPU GEMM (quant-aware,
  OpenMP threads); dispatch table backend-agnostic.
- Vulkan backend: the real breadth answer. Decision needed: replicate CUDA-graph
  capture semantics via vkCmd buffers, or restructure engine around explicit
  command recording. Big design doc required before code.
- Metal/ROCm: after Vulkan proves the abstraction layer.

## Session economics (2026-08-27 onward)
Token budget scarce: work INLINE, no subagent fleets. All verification through
committed gates (./scripts/verify.sh all). New bugs → minimal repro into
tests/, fix, commit. Plans live here; execute top-down without re-exploration.

## REALITY CHECK (2026-08-26, fair CUDA-vs-CUDA scoreboard)
oracle/llama.cpp rebuilt with -DGGML_CUDA=ON (build-cuda/). Honest numbers,
tg-128 both sides GPU (data/bench/results_scoreboard_cuda.jsonl):
  smollm2-135m q4_0: ours 346.7 vs 442.8 (0.78x)
  smollm2-135m q8_0: 143.0 vs 466.1 (0.31x)
  qwen2.5-0.5b q4_0: 222.8 vs 279.3 (0.80x)
  qwen3-0.6b  q8_0:  48.7 vs 194.3  (0.25x)
  llama-3.2-1b q4_0: 58.6 vs 183.0  (0.32x)
PREFILL: oracle ~10k tok/s vs ours ~100 = 100x DEFICIT.
Caveat: absolute values depressed by concurrent agent load on the box;
ratios directionally valid.

### REVISED M9 PRIORITIES (supersedes above order)
1. Batched prefill (pp 100x gap — biggest deficit by far). Proto done,
   integrate dispatch rule T>=8 -> batched GEMM path.
2. Decode kernel scaling: our warp-GEMV loses as model grows (0.80->0.25).
   Study llama.cpp MMQ dequant-in-register kernels; consider split-K GEMV
   or wider per-block tiling for M>=1b-class models.
3. PLE-fused V2 (22% budget) + graph capture re-enable.
4. Split-K flash (12x long ctx) — matters once ctx grows.
5. Arena loader (5.7x startup).
Honest position: decode competitive ONLY at <=135m-class today; "beats
llama.cpp" requires items 1+2 before any marketing.
